<form method="post" action="{{ path('lkpzoospecies_aves_delete', {'id': lkpzoospecies_afe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lkpzoospecies_afe.id) }}">
    <button class="btn">Delete</button>
</form>
